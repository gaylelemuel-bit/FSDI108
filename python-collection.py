"""
python collecions comparison

1.create a list of data structures
write down the four python collecions:
-list
-dictionary
-set
-tuple

2.provide a syntax example
for each collection, write a short python code example that shoes it looks:
    -list
    -dictionary
    -set
    -tuple

    3. anaylze the characteristics
    decide for each collection:
    -is it ordered( does it keep the order element)?
    -is it mutable (can you change it after creatio)?
    -doea it allow duplicates?

    4. identify the access method
    explain hoe to access elements:
    -list/tuple
    -dictionary
    -set
    

    5. oragnize the info in the table

    data structure | syntax example.                         | ordered | mutable | allows duplicates | access method
    list.          |  my_list =[10, 20, 30].                 | yes     | yes     | yes               | list[2]
    dictionary     |  my_dict = {"bob": 31, "joe": 32}.      | yes     | yes     | no                | key
    set            |  my_set = {10, 20, 30}.                 | no      | yes     | no                | if 20 in my_set: print("20 is in the set")
    tuple          |  my_tuple = (10, 20, 30)                | yes     | no      | yes               | tuple[0]

    """
    